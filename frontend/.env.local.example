# Public — exposed to the browser (used for direct fetch calls if any)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-side only — used by Next.js rewrites / server components
BACKEND_URL=http://localhost:8000
