[pytest]
asyncio_mode = auto
# Scoped to tests/ on purpose. evaluation/ must NOT be collected here: those
# scripts need model weights, an HF token and an Anthropic API key, take minutes,
# and report numbers rather than assert pass/fail. See evaluation/README.md.
testpaths = tests
norecursedirs = evaluation .venv alembic uploads
